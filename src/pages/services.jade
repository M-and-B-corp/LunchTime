extends layout
include ../blocks/button/button
include ../blocks/header/header
include ../blocks/session/session
include ../blocks/menuitem/menuitem
include ../blocks/offers/offers
include ../blocks/footer/footer

block title
    title Выбор места заказа
block content
    +header(user)
    .main-wrapper
        .main-wrapper__carry
            each service in services
                +offers(service, "")
                    +button("Сделать заказ", "offers-plugin__button", {href: "/services/" + service._id})
        +footer('© M&B Corporation')